import 'package:flutter/material.dart';

/// Kolory bazowe aplikacji (spójne z ekranem logowania i klimatem AIE).
class AppColors {
  static const background = Color(0xFF0B132B); // deep navy
  static const background2 = Color(0xFF1B2433); // gradient bottom
  static const surface = Color(0xFF1C2541); // cards
  static const surface2 = Color(0xFF2E3A59); // gradient top

  static const primary = Color(0xFF3A86FF); // blue glow
  static const accent = Color(0xFF4CC9F0); // cyan highlight

  static const text = Color(0xFFEAEAEA);
  static const textMuted = Color(0xFFB9C0D4);
}
